#!/bin/bash
set -eu
stream="${2:-}"
if [[ -n $stream ]]; then
    filter="| select(.tap_stream_id == \"$stream\")"
else
    filter=""
fi
$1 -c config.json -d | jq ".streams[]$filter.schema.selected = true"
